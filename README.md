# Clinic-paitent-visit-system-in-Java
in this brannch we added the project phase 2 files which is a simple UI that allowes the user to add patient , clinic, incident data easily with interactable buttons and stuff which then the data would be saved to a text file 
to run this code it is required to have javaFx17 installed into the IDE .
